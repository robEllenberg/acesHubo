program main{

var float LKP = 0.0;

    while(true)
    {

        if(getLine()) then
        {
            LKP = checkCtrl("LKP");
        }


        addCtrl("LKP", LKP);
        addCtrl("LHP", LKP);

        sendCtrl();
        yield;
    }
}
